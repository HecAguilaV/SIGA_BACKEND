[phases.setup]
nixPkgs = ["gradle", "jdk21"]

[phases.install]
cmds = ["gradle build -x test --no-daemon"]

[phases.build]
cmds = ["echo 'Build completed in install phase'"]

[start]
cmd = "./gradlew run --no-daemon"

