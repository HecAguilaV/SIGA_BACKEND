spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

logging:
  level:
    root: WARN
    com.siga.backend: INFO
    org.springframework: WARN
    org.springframework.security: WARN

